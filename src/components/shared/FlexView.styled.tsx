import styled from "styled-components";
import backdrop from "../../assets/images/banners/backdrop.png";

export const FlexView = styled.div`
  background-image: url(${backdrop});
  display: flex;
  position: relative;
  text-align: center;
`;
